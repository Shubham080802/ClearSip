# Versioned package-label catalog

ClearSip will model manufacturer, beverage family, variant, product package, label version, and source record separately. A package's label can change, and a beverage variant can have several sizes or regional formulations; retaining the sourced label version prevents a later formula from silently overwriting what a consumer scanned.
